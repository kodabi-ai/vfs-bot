# VFS Global Automation Configuration
# -----------------------------------
# This file holds configuration values. Sensitive data can be overridden via .env variables.

vfs_global:
  urls:
    base: "https://visa.vfsglobal.com/tur/tr/"
    login: "https://visa.vfsglobal.com/tur/tr/fra/login"
    api_base: "https://lift-api.vfsglobal.com"
  
  credentials:
    email: "{{VFS_EMAIL}}"  # Overridden by VFS_EMAIL env var
    password: "{{VFS_PASSWORD}}" # Overridden by VFS_PASSWORD env var
    phone: "{{VFS_PHONE}}"  # Overridden by VFS_PHONE env var

  timeouts:
    cloudflare: 45000
    content_await: 40000
    form_fill: 10000
    otp_poll: 300
  
  retry:
    max_retries: 3
    delay_seconds: 5

  selectors:
    cf_iframe: "iframe[src*='challenges/']"
    email_input: "input[type='text'], input[type='email']"
    password_input: "input[type='password']"
    login_button: 'button:has-text("Giriş Yap"), button[type="submit"]'
    otp_trigger: 'button:has-text("Gönder"), button:has-text("Send OTP")'
